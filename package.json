{
  "name": "spells-timer",
  "version": "0.0.0",
  "description": "Time ennemies summoner spells with your voice.",
  "main": "index.js",
  "scripts": {
    "build:sass": "node-sass sass/index.scss --output-style compressed --source-map-embed --output www --include-path ./node_modules",
    "build:app": "browserify app/index.jsx -t [ babelify --presets [ es2015 react stage-0 ] ] --debug --sourceMapRelative -o www/index.js",
    "build": "npm run build:sass -s; npm run build:app -s",
    "watch:sass": "npm run build:sass -s -- --watch --recursive",
    "watch:app": "watchify app/index.jsx -t [ babelify --presets [ es2015 react stage-0 ] ] --debug -o www/index.js",
    "watch:emulate": "onchange www/*.{js,css,html} -- cordova emulate",
    "watch:serve": "browser-sync start --server www --files www --no-open --no-ghost-mode",
    "browser": "npm run watch:sass -s & npm run watch:app -s & npm run watch:serve -s",
    "simulator": "npm run watch:sass -s & npm run watch:app -s & npm run watch:emulate -s",
    "start": "npm run build -s && cordova run"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0",
    "babelify": "^7.2.0",
    "browser-sync": "^2.11.1",
    "browserify": "^13.0.0",
    "classnames": "^2.2.3",
    "clj-fuzzy": "^0.3.1",
    "cordova-plugin-splashscreen": "^4.0.0",
    "dominant-color": "0.0.1",
    "eventemitter3": "^2.0.2",
    "fastclick": "^1.0.6",
    "flummox": "^3.6.8",
    "got": "^6.2.0",
    "lol-champions": "^6.21.1",
    "lol-spells": "^6.21.1",
    "node-sass": "^3.4.2",
    "node-uuid": "^1.4.7",
    "onchange": "^3.0.2",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-mixin": "^3.0.3",
    "react-tappable": "^0.8.0",
    "sass-dry": "^0.3.0",
    "watchify": "^3.7.0"
  }
}
